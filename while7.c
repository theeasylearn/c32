// write a program that print even numbers

#include <stdio.h>
void main()
{
      int number = -2;

      while (number<20)
      {
            number = number + 2;
            printf("\n%d", number);
      }

      // number=number+2;
      // printf("%d",number);

      // number=number+2;
      // printf("%d",number);

      // number=number+2;
      // printf("%d",number);
}